<%= render "users/user_info" %>
<%= render "books/new.book" %>

<h1>Book detail</h1>
<% if @book.user.image.attached? %>
  <%= image_tag @book.user.image, size: "200x200" %>
<% else %>
  <%= image_tag 'no_image', size: "200x200" %>
<% end %>
<%= link_to book_path(@book) %>

